name: shirasunakondatev4app
description: A app displaying kondate information for Shirasuna(NITYC).

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2
  grpc: ^3.0.2
  tuple: ^2.0.0
  marquee_widget: ^1.2.0
  sqflite: ^2.0.0+4
  flutter_spinkit: ^5.1.0
  url_launcher: ^6.0.9
  shared_preferences: ^2.0.7
  settings_ui: ^1.0.0
  day_night_time_picker: ^1.0.3
  sprintf: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
